name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            arch: x86_64
            triplet: x64-linux
            artifact_name: chh1dd3n3r-linux-x86_64
          - os: ubuntu-24.04-arm
            arch: aarch64
            triplet: arm64-linux
            artifact_name: chh1dd3n3r-linux-aarch64
          - os: macos-14
            arch: arm64
            triplet: arm64-osx
            artifact_name: chh1dd3n3r-macos-arm64
          - os: macos-13
            arch: x86_64
            triplet: x64-osx
            artifact_name: chh1dd3n3r-macos-x86_64
          - os: windows-latest
            arch: x86_64
            triplet: x64-windows
            artifact_name: chh1dd3n3r-windows-x86_64
    runs-on: ${{ matrix.os }}
    env:
      VCPKG_LIBRARY_LINKAGE: static
      VCPKG_DEFAULT_TRIPLET: ${{ matrix.triplet }}
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake ninja-build perl

      - name: Install system dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install cmake ninja perl

      - name: Setup vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg.git
          if [ "$RUNNER_OS" == "Windows" ]; then
            .\vcpkg\bootstrap-vcpkg.bat
            .\vcpkg\vcpkg.exe install --triplet ${{ matrix.triplet }}
          else
            ./vcpkg/bootstrap-vcpkg.sh
            ./vcpkg/vcpkg install --triplet ${{ matrix.triplet }}
          fi
          echo "VCPKG_ROOT=${{ github.workspace }}/vcpkg" >> $GITHUB_ENV
          echo "VCPKG_DEFAULT_TRIPLET=${{ matrix.triplet }}" >> $GITHUB_ENV
          echo "VCPKG_LIBRARY_LINKAGE=static" >> $GITHUB_ENV

      - name: Configure CMake (Windows)
        if: runner.os == 'Windows'
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DSTATIC_LINKING=ON -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake"

      - name: Configure CMake (Linux/macOS)
        if: runner.os != 'Windows'
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DSTATIC_LINKING=ON -DCMAKE_TOOLCHAIN_FILE="$VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake"

      - name: Build
        run: cmake --build build --config Release

      - name: Package (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist
          cp build/chh1dd3n3r dist/
          tar -czf ${{ matrix.artifact_name }}.tar.gz -C dist chh1dd3n3r

      - name: Package (Windows)
        if: runner.os == 'Windows'
        run: |
          mkdir dist
          copy build\Release\chh1dd3n3r.exe dist\
          Compress-Archive -Path dist\* -DestinationPath ${{ matrix.artifact_name }}.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_name }}.*

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**
          generate_release_notes: true